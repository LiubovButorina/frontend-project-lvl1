name: Linter

# Trigger the workflow on push 
on:
  - push

jobs:
  build:

    # The type of machine to run the job on
    runs-on: ubuntu-latest

    steps: 
      - name: Run linter
        # Run Linter
        run: |
          make lint
      
